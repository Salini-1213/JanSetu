import { useEffect, useState } from "react";
import { ArrowRight, BarChart3, Building2, Check, ChevronLeft, ChevronRight, CircleHelp, FileText, Globe2, Handshake, Lightbulb, MapPin, Menu, Network, Plus, Search, ShieldCheck, Sparkles, Target, Upload, Users, X } from "lucide-react";
import { getDashboardStats, getIndustryMatches, getProblemById, getProblems, getUniversityMatches } from "./services/jansetuApi";
import { problems } from "./data/jansetuMockData";
import DomainExplorer from "./components/DomainExplorer";
import DisasterSidebar from "./components/DisasterSidebar";
import DisasterServices from "./pages/DisasterServices";
import "./jansetu.css";

const journey = [{ icon: FileText, title: "Report", text: "Share a real challenge from your community." }, { icon: Sparkles, title: "AI Understands", text: "Domains, urgency and duplicates are identified." }, { icon: ShieldCheck, title: "Verify", text: "Authorities validate the challenge on the ground." }, { icon: Target, title: "Match", text: "The right university and experts are found." }, { icon: Lightbulb, title: "Innovate", text: "Student and faculty teams build a pathway." }, { icon: Handshake, title: "Collaborate", text: "Industry and CSR partners join the work." }, { icon: Network, title: "Deploy", text: "Field-tested solutions reach communities." }, { icon: BarChart3, title: "Measure", text: "Impact is tracked beyond launch." }];
const demo = { title: "Frequent flooding near a school during monsoon", description: "Heavy rain leaves the school approach road underwater for days, disrupting access for students and staff.", state: "Assam", district: "Kamrup Metropolitan", people: "1500", urgency: "High", affected: ["Students", "Children", "Local residents"], duration: "1–5 years" };

function Navbar({ navigate }) { const [open, setOpen] = useState(false); return <header className="jn-nav"><button className="jn-logo" onClick={() => navigate("/")}><span className="jn-logo-mark"><Network size={17} /></span> JAN<span>SETU</span></button><nav className={open ? "open" : ""}><button onClick={() => navigate("/")}>Home</button><button onClick={() => navigate("/challenges")}>Challenges</button><button onClick={() => navigate("/about")}>How it works</button><button onClick={() => navigate("/impact")}>Impact</button><button onClick={() => navigate("/about")}>About</button></nav><div className="jn-nav-actions"><button className="jn-login" onClick={() => navigate("/dashboard")}>Dashboard</button><button className="jn-nav-cta" onClick={() => navigate("/report")}><Plus size={15} /> Report a problem</button><button className="jn-menu" onClick={() => setOpen(!open)}><Menu size={21} /></button></div></header>; }
function Button({ children, onClick, secondary = false }) { return <button className={secondary ? "jn-btn secondary" : "jn-btn"} onClick={onClick}>{children}<ArrowRight size={16} /></button>; }
function Kpis({ items }) { return <div className="jn-kpis">{items.map(({ label, value, trend }) => <div className="jn-kpi" key={label}><span>{label}</span><strong>{value}</strong>{trend && <small>{trend} this quarter</small>}</div>)}</div>; }
function Footer({ navigate }) { return <footer className="jn-footer"><div><button className="jn-logo" onClick={() => navigate("/")}><span className="jn-logo-mark"><Network size={17} /></span> JAN<span>SETU</span></button><p>From people's problems to India's solutions.</p></div><div><b>Platform</b><button onClick={() => navigate("/challenges")}>Explore challenges</button><button onClick={() => navigate("/report")}>Report a problem</button></div><div><b>Network</b><button onClick={() => navigate("/universities")}>Universities</button><button onClick={() => navigate("/industry")}>Industry connect</button></div></footer>; }
function Landing({ navigate }) { return <><section className="jn-hero"><div className="jn-hero-copy"><div className="jn-eyebrow"><Globe2 size={14} /> India's community innovation network</div><h1>Turn local problems<br /><em>into national solutions.</em></h1><p>JanSetu connects citizens, government, universities, innovators and industry to transform real-world challenges into scalable solutions.</p><div className="jn-hero-actions"><Button onClick={() => navigate("/report")}>Report a Problem</Button><Button secondary onClick={() => navigate("/challenges")}>Explore Challenges</Button></div><div className="jn-trust"><ShieldCheck size={16} /> Built for every state and union territory of India</div></div><div className="jn-ecosystem"><div className="jn-orbit orbit-a" /><div className="jn-orbit orbit-b" /><div className="jn-ecosystem-core"><Network size={30} /><b>JANSETU</b><span>the bridge</span></div>{[{ label: "2,847", text: "Problems reported", pos: "p1" }, { label: "1,426", text: "Challenges verified", pos: "p2" }, { label: "382", text: "University projects", pos: "p3" }, { label: "96", text: "Solutions deployed", pos: "p4" }].map((item) => <div className={`jn-float ${item.pos}`} key={item.text}><strong>{item.label}</strong><span>{item.text}</span></div>)}</div></section><section className="jn-section jn-impact-strip"><div className="jn-section-intro"><span className="jn-eyebrow">A growing national network</span><h2>Problems are everywhere.<br /><em>So are the people who can solve them.</em></h2><small>Illustrative platform metrics</small></div><Kpis items={[{ label: "Problems Reported", value: "12,480+" }, { label: "Districts Reached", value: "186+" }, { label: "Universities Connected", value: "142+" }, { label: "Industry Partners", value: "327+" }, { label: "Solutions Deployed", value: "214" }, { label: "People Benefited", value: "2.4M+" }]} /></section><section className="jn-section"><div className="jn-section-heading"><span className="jn-eyebrow">The JanSetu journey</span><h2>From a voice in the community<br />to measurable <em>impact.</em></h2></div><div className="jn-journey">{journey.map(({ icon: Icon, title, text }, index) => <div className="jn-journey-item" key={title}><div className="jn-journey-icon"><Icon size={19} /></div><span>0{index + 1}</span><b>{title}</b><p>{text}</p>{index < journey.length - 1 && <div className="jn-journey-line" />}</div>)}</div></section><section className="jn-section jn-demo-band"><div><span className="jn-eyebrow">See the bridge in action</span><h2>A citizen reports flooding.<br /><em>An ecosystem builds a solution.</em></h2><p>JanSetu turns “who can solve this?” into a connected pathway from insight to deployment.</p><Button onClick={() => navigate("/challenges/IND-2026-000118")}>View the demo challenge</Button></div><div className="jn-demo-flow"><span>Citizen</span><ArrowRight /><span>AI</span><ArrowRight /><span>University</span><ArrowRight /><span>Industry</span><ArrowRight /><span className="done">Impact</span></div></section></> }

function Wizard({ navigate }) { const [step, setStep] = useState(1); const [data, setData] = useState({ ...demo }); const [files, setFiles] = useState([]); const [submitted, setSubmitted] = useState(false); const update = (key, value) => setData({ ...data, [key]: value }); const next = () => step < 5 ? setStep(step + 1) : setSubmitted(true); if (submitted) return <Success navigate={navigate} />; const steps = ["Problem", "Location", "Impact", "Evidence", "Review"]; return <div className="jn-wizard"><div className="jn-wizard-top"><div><span className="jn-eyebrow">Problem submission</span><h1>Help us understand the challenge.</h1></div><span className="jn-step-label">Step {step} of 5</span></div><div className="jn-stepper">{steps.map((name, index) => <button key={name} className={step >= index + 1 ? "active" : ""} onClick={() => index + 1 < step && setStep(index + 1)}><span>{step > index + 1 ? <Check size={13} /> : `0${index + 1}`}</span>{name}</button>)}</div><div className="jn-wizard-card">{step === 1 && <><span className="jn-eyebrow">Tell us about the problem</span><h2>What problem are you facing?</h2><p className="jn-subtitle">You don't need to know the solution. Just help us understand the challenge.</p><label>Problem title <b>*</b><input maxLength="120" value={data.title} onChange={e => update("title", e.target.value)} placeholder="Example: Frequent flooding near a school during monsoon" /><small>{data.title.length} / 120</small></label><label>Description <b>*</b><textarea maxLength="1000" rows="7" value={data.description} onChange={e => update("description", e.target.value)} placeholder="Describe what is happening, who is affected, where it happens and why it matters." /><small>{data.description.length} / 1000</small></label><div className="jn-ai-card"><Sparkles size={21} /><div><b>AI Insight</b><p>After submission, JanSetu AI will identify relevant domains, urgency and similar existing challenges.</p></div></div></>}{step === 2 && <><span className="jn-eyebrow">Pinpoint the place</span><h2>Where is the problem?</h2><p className="jn-subtitle">Location helps us understand scale and identify nearby institutions that may help.</p><div className="jn-form-grid"><label>Country<input value="India" readOnly /></label><label>State / Union Territory <b>*</b><input value={data.state} onChange={e => update("state", e.target.value)} /></label><label>District <b>*</b><input value={data.district} onChange={e => update("district", e.target.value)} /></label><label>City / Block<input placeholder="Enter city or block" /></label><label>Village / Locality<input placeholder="Enter locality" /></label><label>PIN Code<input placeholder="6-digit PIN" /></label></div><button className="jn-location"><MapPin size={17} /> Use My Current Location</button><div className="jn-map"><MapPin size={25} /><span>Leaflet + OpenStreetMap preview</span><small>Your exact location is shared only with authorized users.</small></div></>}{step === 3 && <><span className="jn-eyebrow">Show the scale</span><h2>How big is the impact?</h2><p className="jn-subtitle">Help us understand the people and communities affected.</p><label>Approximate people affected <input type="number" value={data.people} onChange={e => update("people", e.target.value)} /></label><div className="jn-select-label">Who is affected?</div><div className="jn-choice-grid">{["Farmers", "Students", "Children", "Women", "Elderly", "Persons with Disabilities", "Local Residents", "Workers", "Businesses", "Healthcare Workers"].map(item => <button type="button" className={data.affected.includes(item) ? "selected" : ""} key={item} onClick={() => update("affected", data.affected.includes(item) ? data.affected.filter(x => x !== item) : [...data.affected, item])}>{data.affected.includes(item) && <Check size={14} />}{item}</button>)}</div><div className="jn-form-grid"><label>Urgency<select value={data.urgency} onChange={e => update("urgency", e.target.value)}>{["Low", "Medium", "High", "Critical"].map(x => <option key={x}>{x}</option>)}</select></label><label>Problem duration<select value={data.duration} onChange={e => update("duration", e.target.value)}>{["Less than 1 month", "1–6 months", "6–12 months", "1–5 years", "5+ years"].map(x => <option key={x}>{x}</option>)}</select></label></div></>}{step === 4 && <><span className="jn-eyebrow">Add evidence</span><h2>Help us understand the problem better.</h2><p className="jn-subtitle">Photos, videos and documents help institutions understand the challenge.</p><label className="jn-upload"><Upload size={23} /><b>Drop files here or browse</b><span>JPG, PNG, MP4, PDF, DOC · Max 10 MB each</span><input type="file" multiple onChange={e => setFiles([...e.target.files])} /></label>{files.length > 0 && <div className="jn-file-list">{files.map(file => <div key={file.name}><FileText size={16} />{file.name}<small>{(file.size / 1024).toFixed(0)} KB</small><X size={15} onClick={() => setFiles(files.filter(x => x.name !== file.name))} /></div>)}</div>}<div className="jn-idea-card"><Lightbulb size={20} /><div><b>Do you have an idea for solving this?</b><p>You can share it at the next step, or our university partners can help explore solutions.</p></div></div></>}{step === 5 && <Review data={data} files={files} navigate={navigate} />}</div><div className="jn-wizard-actions">{step > 1 && <button className="jn-back" onClick={() => setStep(step - 1)}><ChevronLeft size={17} /> Back</button>}<button className="jn-btn" onClick={next}>{step === 5 ? "Submit Problem" : "Continue"}<ChevronRight size={17} /></button></div></div>; }
function Review({ data, files, navigate }) { return <><span className="jn-eyebrow">Review & submit</span><h2>Ready to send this challenge forward?</h2><p className="jn-subtitle">Review your report before it enters the JanSetu innovation network.</p><div className="jn-review-grid"><div><span>Problem</span><b>{data.title}</b><p>{data.description}</p></div><div><span>Location</span><b>{data.district}, {data.state}</b><p>India · Exact location can be added later</p></div><div><span>Impact</span><b>{data.people || "Not provided"} people · {data.urgency} priority</b><p>{data.affected.join(", ") || "Community members"}</p></div><div><span>Evidence</span><b>{files.length ? `${files.length} attachment(s)` : "No attachments"}</b><p>All files are checked before upload.</p></div></div><label className="jn-declaration"><input type="checkbox" defaultChecked /> I confirm this information is accurate to the best of my knowledge.</label><label className="jn-declaration"><input type="checkbox" defaultChecked /> I agree that authorized partners may review this problem to develop a solution.</label></>; }
function Success({ navigate }) { return <div className="jn-success"><div className="jn-success-check"><Check size={34} /></div><span className="jn-eyebrow">Submission received</span><h1>Your voice has been <em>heard.</em></h1><p>Your challenge is now part of the JanSetu innovation network.</p><div className="jn-id-card"><span>Problem ID</span><strong>IND-2026-000001</strong><span>Status</span><b>Submitted</b></div><div className="jn-mini-timeline">{["Submitted", "AI Analysis", "Verification", "Institution Matching", "Solution Development", "Deployment"].map((x, i) => <div className={i === 0 ? "active" : ""} key={x}><span>{i === 0 ? <Check size={12} /> : i + 1}</span><b>{x}</b></div>)}</div><div className="jn-hero-actions"><Button onClick={() => navigate("/dashboard")}>Go to Dashboard</Button><Button secondary onClick={() => navigate("/report")}>Submit Another</Button></div></div> }

function Challenges({ navigate }) { const [items, setItems] = useState([]); useEffect(() => { getProblems().then(setItems); }, []); return <Page title="Explore India's Challenges" eyebrow="Verified challenges" intro="Discover the real-world problems communities are facing, and the opportunities waiting for ideas." navigate={navigate}><div className="jn-search"><Search size={18} /><input placeholder="Search problems, districts, domains..." /></div><div className="jn-filter-row"><button className="selected">All challenges</button><button>High priority</button><button>Water & climate</button><button>Healthcare</button><button>Education</button></div><div className="jn-challenge-grid">{items.map(p => <ProblemCard key={p.id} problem={p} navigate={navigate} />)}</div></Page>; }
function ProblemCard({ problem, navigate }) { return <article className="jn-problem-card"><div className={`jn-card-art ${problem.color}`}><span>{problem.priority} priority</span><MapPin size={31} /></div><div className="jn-card-body"><small>{problem.id} · {problem.date}</small><h3>{problem.title}</h3><p><MapPin size={14} /> {problem.location}</p><div className="jn-card-tags">{problem.domains.map(x => <span key={x}>{x}</span>)}</div><div className="jn-card-foot"><span>{problem.people} affected</span><b>{problem.status}</b></div><button className="jn-text-button" onClick={() => navigate(`/challenges/${problem.id}`)}>View challenge <ArrowRight size={14} /></button></div></article>; }
function Dashboard({ navigate }) { return <Page title="Good evening, Citizen" eyebrow="Your JanSetu dashboard" intro="Follow the challenges you have raised and the solutions taking shape around them." navigate={navigate}><Kpis items={[{ label: "Problems Reported", value: "03" }, { label: "Under Review", value: "01" }, { label: "Matched", value: "01" }, { label: "Solutions Deployed", value: "00" }]} /><div className="jn-dashboard-heading"><h2>My problems</h2><button className="jn-text-button" onClick={() => navigate("/report")}>Report another <Plus size={14} /></button></div><div className="jn-table">{[problems[1], problems[0]].map(p => <ProblemCard key={p.id} problem={p} navigate={navigate} />)}</div></Page>; }
function Details({ navigate }) { const [p, setP] = useState(null); useEffect(() => { getProblemById("IND-2026-000118").then(setP); }, []); if (!p) return <Page title="Loading challenge..." eyebrow="JanSetu" navigate={navigate}><div className="jn-loading" /></Page>; return <Page title={p.title} eyebrow={p.id} intro={p.location} navigate={navigate}><div className="jn-detail-layout"><div><section className="jn-white-card"><span className="jn-eyebrow">Problem description</span><p className="jn-detail-copy">{p.description} The community has requested a reliable, affordable intervention that can be maintained locally.</p></section><section className="jn-white-card"><span className="jn-eyebrow">Solution progress</span><div className="jn-detail-timeline">{["Submitted", "AI Analysis", "Verification", "Institution Matching", "Prototype Development", "Field Testing", "Deployment"].map((x, i) => <div className={i < 4 ? "done" : i === 4 ? "current" : ""} key={x}><span>{i < 4 ? <Check size={12} /> : i + 1}</span><b>{x}</b><small>{i === 3 ? "IIT Guwahati matched" : i === 4 ? "In progress" : "Complete"}</small></div>)}</div></section></div><aside><AI /><section className="jn-white-card"><span className="jn-eyebrow">Impact snapshot</span><div className="jn-big-stat">1,500+<small>people affected</small></div><div className="jn-card-tags"><span>Students</span><span>Local residents</span></div></section></aside></div><div className="jn-detail-links"><button onClick={() => navigate("/universities")}>View institution matches <ArrowRight size={15} /></button><button onClick={() => navigate("/industry")}>See industry interest <ArrowRight size={15} /></button></div></Page>; }
function AI() { return <section className="jn-ai-panel"><div className="jn-ai-title"><Sparkles size={17} /> JANSETU AI ANALYSIS</div><h3>Urban Development<br /><em>+ Water Resources</em></h3>{[["Priority", "High"], ["AI confidence", "92%"], ["Similar challenges", "3"]].map(([x, y]) => <div className="jn-ai-row" key={x}><span>{x}</span><b>{y}</b></div>)}<div className="jn-progress-bar"><i /></div><small>Suggested expertise: hydrology, IoT, civil engineering</small></section>; }
function NetworkPage({ type, navigate }) { const [items, setItems] = useState([]); useEffect(() => { (type === "university" ? getUniversityMatches() : getIndustryMatches()).then(setItems); }, [type]); return <Page eyebrow={type === "university" ? "Research network" : "Collaboration network"} title={type === "university" ? "Institutions that can help" : "Industry Connect"} intro={type === "university" ? "Match real challenges with faculty expertise, research facilities and student teams." : "Bring funding, mentorship, prototyping and deployment capacity to the problems that matter."} navigate={navigate}><div className="jn-network-grid">{items.map(item => <article className="jn-network-card" key={item.name}><div className="jn-network-icon">{type === "university" ? <Building2 size={22} /> : <Handshake size={22} />}</div><span className="jn-eyebrow">{item.type || "University"}</span><h3>{item.name}</h3><p><MapPin size={14} /> {item.location || item.expertise}</p>{type === "university" && <><div className="jn-match"><strong>{item.score}%</strong> Match score</div><div className="jn-card-tags">{item.expertise.map(x => <span key={x}>{x}</span>)}</div></>}{type !== "university" && <><p>{item.expertise}</p><div className="jn-support">{item.support.map(x => <span key={x}>{x}</span>)}</div></>}<button className="jn-text-button">View profile <ArrowRight size={14} /></button></article>)}</div></Page>; }
function Impact({ navigate }) { const [stats, setStats] = useState(null); useEffect(() => { getDashboardStats().then(setStats); }, []); return <Page eyebrow="Platform intelligence" title="Impact you can see." intro="A transparent view of the problems, people and partnerships moving through the JanSetu network." navigate={navigate}>{stats && <><Kpis items={stats.kpis} /><div className="jn-analytics"><section className="jn-white-card"><span className="jn-eyebrow">Problems by domain</span><h3>Where communities need action</h3>{stats.domains.map(x => <div className="jn-bar" key={x.label}><span>{x.label}</span><i><b style={{ width: `${x.value}%` }} /></i><strong>{x.value}</strong></div>)}</section><section className="jn-white-card jn-india-placeholder"><Globe2 size={54} /><h3>India's innovation challenge map</h3><p>State-level problem density visualization ready for Leaflet + OpenStreetMap.</p><button className="jn-text-button" onClick={() => navigate("/challenges")}>Explore challenges <ArrowRight size={14} /></button></section></div></>}</Page>; }
function About({ navigate }) { return <Page eyebrow="The JanSetu idea" title="A bridge built for scale." intro="Jan means people. Setu means bridge. Together, JanSetu is a shared path from lived experience to lasting change." navigate={navigate}><div className="jn-about-grid">{journey.slice(0, 6).map(({ icon: Icon, title, text }) => <article className="jn-white-card" key={title}><Icon size={21} className="jn-about-icon" /><h3>{title}</h3><p>{text}</p></article>)}</div><div className="jn-cta-card"><div><span className="jn-eyebrow">Your community knows what matters.</span><h2>Bring the next challenge<br />into the network.</h2></div><Button onClick={() => navigate("/report")}>Report a Problem</Button></div></Page>; }
function Page({ eyebrow, title, intro, children, navigate }) { return <><section className="jn-inner-hero"><span className="jn-eyebrow">{eyebrow}</span><h1>{title}</h1>{intro && <p>{intro}</p>}</section><main className="jn-page-content">{children}</main></>; }

export default function JansetuApp() { const [path, setPath] = useState(window.location.pathname); const navigate = (next) => { window.history.pushState({}, "", next); setPath(next); window.scrollTo(0, 0); }; useEffect(() => { const onPop = () => setPath(window.location.pathname); window.addEventListener("popstate", onPop); return () => window.removeEventListener("popstate", onPop); }, []); let content; if (path === "/report") content = <Wizard navigate={navigate} />; else if (path === "/challenges") content = <Challenges navigate={navigate} />; else if (path.startsWith("/challenges/")) content = <Details navigate={navigate} />; else if (path === "/dashboard") content = <Dashboard navigate={navigate} />; else if (path === "/impact") content = <Impact navigate={navigate} />; else if (path === "/universities") content = <NetworkPage type="university" navigate={navigate} />; else if (path === "/industry") content = <NetworkPage type="industry" navigate={navigate} />; else if (path === "/about") content = <About navigate={navigate} />; else if (path === "/agriculture") content = <DisasterServices type="agriculture" navigate={navigate} />; else if (path === "/rural-development") content = <DisasterServices type="rural" navigate={navigate} />; else if (path === "/health-services") content = <DisasterServices type="health" navigate={navigate} />; else if (path === "/emergency") content = <DisasterServices type="emergency" navigate={navigate} />; else if (path === "/government-services") content = <DisasterServices type="government" navigate={navigate} />; else content = <Landing navigate={navigate} />; return <div className="jn-app jn-shell"><DisasterSidebar path={path} navigate={navigate} /><div className="jn-main-column"><Navbar navigate={navigate} />{content}{path === "/" && <DomainExplorer navigate={navigate} />}<Footer navigate={navigate} /></div></div>; }
